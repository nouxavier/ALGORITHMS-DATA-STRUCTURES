using System;
using Algorithms.Sort;

namespace Algorithms.Test.Sort
{
    public class BigSortBuilder
    {
        public static BigSort NewAllProps(string[] input)
        {
            return new BigSort(input);
        }
    }
}
