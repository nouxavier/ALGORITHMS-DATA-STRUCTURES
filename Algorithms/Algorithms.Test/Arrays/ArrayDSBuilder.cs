using System;
using Algorithms.Arrays;

namespace Algorithms.Test.Arrays
{
    public class ArrayDSBuilder
    {
        public static ArrayDS NewWithAllProps(int[] input)
        {
            return new ArrayDS(input);
        }
    }
}

