using System;
using Algorithms.Arrays;

namespace Algorithms.Test.Arrays
{
    public class LeftRotationBuilder
    {
        public static LeftRotation NewWithAllProps(int[] input)
        {
            return new LeftRotation(input);
        }
    }
}

